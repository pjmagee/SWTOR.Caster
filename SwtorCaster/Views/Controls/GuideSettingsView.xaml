<UserControl x:Class="SwtorCaster.Views.GuideSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="500" Background="White">
    <UserControl.Resources>
      <!-- None -->
    </UserControl.Resources>
    <Grid>
        <TabControl>
            <TabItem Header="Load a guide">
                <ContentControl x:Name="LoadGuideViewModel" HorizontalAlignment="Left" />                
            </TabItem>
            <TabItem Header="Create a guide">
                <ContentControl x:Name="CreateGuideViewModel" HorizontalAlignment="Left" />
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
