<!-- Normal Window so we can capture INNER WINDOW inside OBS -->
<Window x:Class="SwtorCaster.Views.ObsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:SwtorCaster.Views"
        Height="400" Width="600" Icon="../Resources/swtorcaster.ico"
        Background="{Binding BackgroundColor}" ResizeMode="CanResize" 
        AllowsTransparency="False"
        WindowStartupLocation="CenterOwner" x:Name="Window">
    <!-- Caliburn to inject AbilityListView.xaml -->
    <ContentControl x:Name="AbilityListViewModel" />
</Window>
